package com.example.travle_mobile

import io.flutter.embedding.android.FlutterFragmentActivity

// flutter_stripe requires the host Activity to be a FlutterFragmentActivity (the PaymentSheet is a
// DialogFragment) whose theme descends from Theme.AppCompat — see res/values*/styles.xml.
class MainActivity : FlutterFragmentActivity()
